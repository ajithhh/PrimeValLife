@model PrimeValLife.Core.Models.Users.Cart
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

<div class="d-flex align-items-end justify-content-between mb-30">
    
</div>
<div class="d-flex align-items-end justify-content-between">
    <div>
        <div class="table-responsive shopping-summery" id="cart">
            <table class="table table-wishlist">
                @* <thead>
                    <tr class="main-heading">
                        <th class="custome-checkbox start pl-30">
                            <input class="form-check-input" type="checkbox" name="checkbox" id="exampleCheckbox11" value="">
                            <label class="form-check-label" for="exampleCheckbox11"></label>
                        </th>
                        <th scope="col" colspan="2">Product</th>
                        <th scope="col">Unit Price</th>
                        <th scope="col">Quantity</th>
                        <th scope="col">Subtotal</th>
                        <th scope="col" class="end">Remove</th>
                    </tr>
                </thead> *@
                <tbody>
                    @if ( Model!=null && Model.CartItems.Count >0)
                    {
                        @foreach (var item in Model.CartItems)
                        {
                            await Html.RenderPartialAsync("_CartProduct", item);
                        }
                    }
                    else
                    {
                        <tr><td><a href="home/index">Click here to shop</a></td></tr>
                        }
                </tbody>
            </table>
        </div>
        <div class="divider-2 mb-30"></div>
        @* <div class="row mt-50">
            <div class="col-lg-5">
                <div class="p-40">
                    <h4 class="mb-10">Apply Coupon</h4>
                    <p class="mb-30"><span class="font-lg text-muted">Using A Promo Code?</p>
                    <form action="#">
                        <div class="d-flex justify-content-between">
                            <input class="font-medium mr-15 coupon" name="Coupon" placeholder="Enter Your Coupon">
                            <button class="btn"><i class="fi-rs-label mr-10"></i>Apply</button>
                        </div>
                    </form>
                </div>
            </div>
        </div> *@
    </div>
</div>